//import { useTranslation } from 'react-i18next';
const AddressTable = (): React.ReactElement => {
    //const { t } = useTranslation();

    
    return(
        <>

        </>
    );
}

export default AddressTable;